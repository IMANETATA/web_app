{% extends 'base.html.twig' %}

{% block title %}symrecip-accueil{% endblock %}

{% block body %}
<div class="container mt-4">

<div class="jumbotron">
  <h1 class="display-4">Bienvenue sur SymRecip!</h1>
  <p class="lead">SymRecip est une application de  creation des recette .lorem ipsum lorem ipsum lorem ipsum lorem ipsum lorem ipsum</p>
  <hr class="my-4">
  <p>pour commencer . render vous sur la page d inscription pour utiliser lapplication</p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="#" role="button">inscription</a>
  </p>
</div>

<div class="recipes">
<h2 class="mt-4">recette de la communaute</h2>
<div class="d-flex justify-content-between flex-wrap">
{% for recipe in recipes %}
    <div class="card border-primary mb-3" style="max-width: 20rem;">
  <div class="card-header">Recette nº:{{recipe.id}}</div>
  <div class="card-body">
    <h4 class="card-title">{{recipe.name}}</h4>
    <p class="card-text">{{ recipe.description | slice(0,500) ~ '...'  }}</p>
  </div>
</div>
{% endfor %}
</div>
</div>


{% endblock %}
